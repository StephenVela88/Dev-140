I added proximity to my grouped elements with .form-group block to show the labels and error messages that belong to the corresponding inputs.

I matched the new contact information form by putting the entire form inside a <fieldset> with a the matching background color and orange border to visually separate the input collection, but matching the page theme.

The inputs I added made to the structure classes and text areas similar so users instinctively recognizable and will identify them as interactive fields.  